.edit_user
  = form_for :user, :url => update_picture_athletes_path, :method => :put, :html => {:multipart => true}, :remote => true do |f|
    .field
      = f.file_field :picture
    .actions
      = f.submit 'Laden'
      = link_to image_tag("abbrechen.png"), '', 'data-edit' => 'abort'