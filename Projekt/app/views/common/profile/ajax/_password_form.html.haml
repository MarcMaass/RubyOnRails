.edit_user
  = form_for :user, :url => update_password_athletes_path, :method => :put, :remote => true do |f|
    = fields_for :password do |p|
      .field
        = p.text_field :old_password, :value => 'Altes Passwort'
      .field
        = p.text_field :password, :value => 'Neues Passwort'
      .field
        = p.text_field :password_confirmation, :value => 'Neues Passwort bestätigen' 
    .actions
      = f.submit 'Speichern'
      = link_to image_tag('abbrechen.png'), '', 'data-edit' => 'abort'
