// This code-snippet should render a small error div and removes it after one second
:plain
  $('.edit_user').append('#{escape_javascript(render :partial => "common/profile/error")}');
  $('#error').ready(function (event) {
    setTimeout(function() {
      $('#error').fadeOut(function() {
        $(this).remove()
      });
    }, 1000);
  });
