-# From For adding a Sistrictchief
= form_for(@district_chief) do |f|
  - if @district_chief.errors.any?
    #error_explanation
      %h2
        = pluralize(@district_chief.errors.count, "error")
        prohibited this district_chief from being saved:
      %ul
        - @district_chief.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.label :district
    %br/
    = f.text_field :district
  .actions
    = f.submit
